# Sentinel CLAUDE.md (forgecraft-mcp)

- repo: jghiringhelli/forgecraft-mcp@5552913
- feature: 6-line CLAUDE.md sentinel that routes via .claude/index.md to domain standards files

## Why this score

The instruction file is intentionally small (≤200 lines, sentinel-style) and
covers architecture, conventions, decisions, and protocols by routing rather
than repeating. It satisfies all three keyword-coverage signals (architecture,
convention, pattern) through the domain standards files it points to.

## What would move it up

Score 2 is the maximum for self-describing. To move beyond a level-2 anchor,
the project would need automatic verification that the routed standards stay
in sync with the artifacts they describe (e.g. drift detection between
.claude/standards/ and .claude/state.md).
