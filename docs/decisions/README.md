# Decisions

Lightweight one-pager rationale for non-architectural decisions: bug-fix interpretations, operational tweaks, "we chose X over Y because Z" notes that don't warrant a full ADR.

Filename: `YYYY-MM-DD-slug.md`.

Encouraged on `fix:` commits when behavior is intentionally redefined (not just patched). Encouraged on `revert:` always.
