# Active ADRs

Architectural Decision Records currently in force. Filename: `ADR-NNNN-slug.md`.

Each ADR records: context, decision, alternatives rejected, consequences.

When a decision is superseded or rolled back, move the file to `../done/` and add a header line: `Superseded by: ADR-NNNN`.

Legacy ADRs live at `docs/adr/` and `docs/adrs/` — see `docs/manifest.yaml`. New ADRs go here.
