# Schemas

Diagrams and structured schemas: mermaid (`*.mmd` or fenced `*.md`), JSON Schema, OpenAPI, ER diagrams.

`feat:` commits that change data models or API surface should touch a file here.

Legacy diagrams live at `docs/diagrams/` and `docs/schema.md` — see `docs/manifest.yaml`. New diagrams go here.
